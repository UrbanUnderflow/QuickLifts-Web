# Sage Presence Card Notes – Feb 12, 2026

AGENT_PROFILES (virtualOffice.tsx) structure refresher:
- Each agent entry defines `title`, `location`, `sections: { title, bullets }[]`, optional `footer`.
- Nora/Scout/Solara all follow the same schema (3–4 sections, bullets as strings, footer with persona reminder).

Implication for Sage:
- Need to mirror exact schema when adding Sage.
- Card content should highlight intel feed stewardship, field research/listening, insight packaging/escalation, plus creed footer.

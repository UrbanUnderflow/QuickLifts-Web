# Shared Workspace Guide — Partnership Deliverables

## Where to Write
- Use `/Users/noraclawdbot/.openclaw/workspace` (shared repo) for deliverables everyone must access.
- Agent sandboxes (`workspace-scout`, `workspace-solara`, etc.) are for drafting only; copy final files into the shared workspace or push to the shared remote before reporting done.

## Hand-off Steps
1. **Copy/Move Final Files** into `/Users/noraclawdbot/.openclaw/workspace` (shared repo) or push to the central remote so everyone (and Tremaine) sees them.
2. **Link + Verify** — Post the file path/link, have a teammate confirm “✅ opened.”
3. **Log in Standup** — Only after verification.

## If Push Fails / Files Invisible
- Escalate immediately (don’t report “done”).
- Note it in the recap (“Lead tile yellow — no shared files as of HH:MM”).

_This ensures Hampton/Oura/fencing deliverables actually live where every agent (and Tremaine) can access them._

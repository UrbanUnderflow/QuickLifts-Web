# “Brand Director” Reference Scan (Step 1)

Occurrences located via `grep -R "Brand Director"`:

1. `src/components/virtualOffice/AgentChatModal.tsx` (role mapping in `AGENT_ROLES` constant).
2. `src/pages/admin/virtualOffice.tsx`
   - Desk positions comment (“Brand Director — far right, lower”).
   - `AGENT_ROLES.solara` entry.
   - `AGENT_PROFILES.solara.title` and footer copy.
3. `src/pages/admin/agentChat.tsx` — role map for direct chat UI.
4. Docs referencing the title:
   - `docs/kanban-task-plan-2026-02-11.md` (two mentions).
   - `docs/AGENT_ONBOARDING.md` (role listing).

These are the locations to update when renaming Solara’s role to “Brand Voice.”
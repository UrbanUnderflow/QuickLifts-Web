rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access — revisit security rules later
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

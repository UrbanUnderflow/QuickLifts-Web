// Remove Brevo SDK import if no longer used elsewhere in this file
// import Brevo from '@getbrevo/brevo';

// Remove API client initialization if no longer used
// const eventsApi = new Brevo.EventsApi();
// eventsApi.setApiKey(Brevo.EventsApiApiKeys.apiKey, process.env.BREVO_MARKETING_KEY!);

// Remove the old sendBrevoEvent function
/*
export async function sendBrevoEvent(
  emailOrUid: string,
  eventName: string,
  data: Record<string, any> = {}
) {
  // ... old implementation using fetch ...
} 
*/

// This file might be empty now or contain other Brevo-related server-side functions.
// If it's empty, you could consider deleting it, but keep it for now
// in case other server-side Brevo functions are needed later. 